stm32 
1.内部adc1转换，ADC CLK 12MHz,大概1.1us转换1次，Tim2定时触发。
2.DMA传输600次转换值，完成本轮采样，寻找触发点。（后续要采用硬件触发，实现AC、DC耦合，自动扫描）
3.lcd采用硬件SPI2，先用背景色画上一次波形的点，再用前景色画本次采集到的点，闪烁不那么严重了。
4.labview做上位机，串口（DMA）发送本次采集的数据，以及接收PC的命令。
5.系统时间轮片法，Systick定时中断4ms1次，查询是否执行相应的任务。（现在看Ucos，后续准备切换到ucos）
